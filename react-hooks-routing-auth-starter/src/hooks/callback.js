/**
 * component that Auth0 will redirect to after
 * the user authenticates with Auth0. From here
 * user is redirected to the authcheck page
 * then the home page
 */

import React from 'react';

const Callback = props => (
  <div>
    Callback
  </div>
);

export default Callback;
